# 🚀 Guia Completo de Gitflow para a Equipe de Desenvolvimento

Olá, equipe! 👋

Este guia detalhado tem como objetivo apresentar e padronizar o uso do **Gitflow**, um modelo de ramificação (branching model) robusto e amplamente adotado, para otimizar nosso fluxo de desenvolvimento. Compreender e aplicar o Gitflow nos ajudará a manter nosso código organizado, facilitar a colaboração, gerenciar versões de forma eficaz e garantir a estabilidade do nosso software.

---

## 📜 Sumário

* [O que é Gitflow?](#🤔-o-que-é-gitflow)
* [Por que usar Gitflow?](#-por-que-usar-gitflow)
* [As Branches do Gitflow](#🌳-as-branches-do-gitflow)
    * [Branches Principais](#branches-principais)
    * [Branches de Suporte](#branches-de-suporte-temporárias)
* [🔄 Fluxo de Trabalho na Prática com Exemplos](#🔄-fluxo-de-trabalho-na-prática-com-exemplos)
    * [Cenário 1: Iniciando o Projeto](#cenário-1-iniciando-o-projeto)
    * [Cenário 2: Desenvolvendo uma Nova Funcionalidade (`feature`)](#cenário-2-desenvolvendo-uma-nova-funcionalidade-feature)
    * [Cenário 3: Preparando um Release (`release`)](#cenário-3-preparando-um-release-release)
    * [Cenário 4: Corrigindo um Bug Crítico em Produção (`hotfix`)](#cenário-4-corrigindo-um-bug-crítico-em-produção-hotfix)
* [🛠️ Comandos Git Essenciais no Gitflow](#️-comandos-git-essenciais-no-gitflow)
* [✅ Benefícios Resumidos do Gitflow](#✅-benefícios-resumidos-do-gitflow)
* [⚠️ Considerações Importantes](#⚠️-considerações-importantes)
* [🤝 Próximos Passos e Dúvidas](#🤝-próximos-passos-e-dúvidas)

---

## 🤔 O que é Gitflow?

Gitflow é um modelo de ramificação para o Git, proposto por Vincent Driessen. Ele estabelece uma estrutura clara de branches e define como elas devem interagir ao longo do ciclo de vida do desenvolvimento de software. Seu principal objetivo é trazer organização e controle, desde a concepção de novas funcionalidades até o lançamento de versões estáveis e a aplicação de correções urgentes.

### 🌟 Por que usar Gitflow?

* **Desenvolvimento Paralelo Eficaz:** Permite que múltiplos desenvolvedores trabalhem em diferentes funcionalidades simultaneamente, sem causar conflitos diretos no código principal.
* **Colaboração Simplificada:** Facilita a integração do trabalho de diversos membros da equipe de forma estruturada.
* **Histórico de Versões Limpo:** A branch principal (`master`/`main`) reflete apenas as versões de produção, mantendo um histórico claro e significativo.
* **Processo de Release Organizado:** Define um fluxo específico para a preparação e o lançamento de novas versões do software.
* **Correções Rápidas e Seguras:** Proporciona um mecanismo isolado para corrigir bugs críticos em produção sem interferir no desenvolvimento de novas features.

---

## 🌳 As Branches do Gitflow

O Gitflow se baseia em duas branches principais de longa duração e diversas branches de suporte temporárias, cada uma com um propósito bem definido.

### Branches Principais

1.  **`master` (ou `main`)** 🥇
    * **Propósito:** Contém o código que está **em produção**. Cada commit nesta branch representa uma nova versão estável e testada do software.
    * **Tags de Versão:** Cada merge na `master` deve ser acompanhado de uma tag de versão (ex: `v1.0.0`, `v1.0.1`, `v2.0.0`).
    * **Origem:** Geralmente, é a branch padrão criada com o repositório.
    * **Interação:** Recebe merges **exclusivamente** de branches `release` ou `hotfix`.
    > **Importante:** Nunca se deve fazer commit diretamente na branch `master`. Ela é o reflexo fiel do que está em produção.

2.  **`develop`** 🛠️
    * **Propósito:** É a branch de **integração contínua**. Aqui são consolidadas todas as funcionalidades que foram desenvolvidas e aprovadas, representando o estado mais atual do código que está sendo preparado para o próximo release.
    * **Origem:** Criada a partir da `master`.
    * **Interação:** Recebe merges de branches `feature` e, ocasionalmente, `hotfix`. É a base para a criação de branches `release`.
    > **Importante:** Assim como na `master`, evite commits diretos na `develop`. As alterações devem vir de merges de outras branches.

### Branches de Suporte (Temporárias)

Estas branches têm um ciclo de vida limitado: são criadas para uma finalidade específica e, uma vez cumprido seu objetivo, são excluídas.

1.  **`feature/*` (ex: `feature/autenticacao-jwt`)** ✨
    * **Propósito:** Desenvolver **novas funcionalidades** de forma isolada.
    * **Origem:** Sempre criada a partir da branch `develop`.
    * **Destino (Merge):** Mesclada de volta na `develop` após a conclusão e testes.
    * **Convenção de Nomenclatura:** `feature/nome-descritivo-da-funcionalidade` (ex: `feature/novo-dashboard-usuario`, `feature/integracao-api-pagamentos`).
    * **Ciclo de Vida:** Existe apenas durante o desenvolvimento da funcionalidade. Após o merge para `develop`, pode ser excluída (local e remotamente).

2.  **`release/*` (ex: `release/v1.2.0`)** ემ
    * **Propósito:** Preparar uma **nova versão para produção**. Nesta branch, o foco é em estabilização: são realizadas apenas correções de bugs menores, ajustes finais, atualizações de documentação e metadados para o lançamento (como o número da versão).
    * **Origem:** Criada a partir da `develop` quando esta atinge um estado desejado e estável para um novo release.
    * **Destino (Merge):**
        * Após a finalização e aprovação, é mesclada na `master` (e recebe uma tag de versão).
        * Também é mesclada de volta na `develop` para garantir que quaisquer correções feitas durante a fase de release sejam incorporadas ao fluxo principal de desenvolvimento.
    * **Convenção de Nomenclatura:** `release/versao-do-release` (ex: `release/v1.0.0`, `release/v2.1.3`).
    * **Ciclo de Vida:** Existe enquanto a versão está sendo preparada. Após os merges para `master` e `develop`, é excluída.
    > **Atenção:** Nenhuma nova funcionalidade deve ser adicionada a uma branch `release`. O foco é apenas na preparação do que já foi desenvolvido.

3.  **`hotfix/*` (ex: `hotfix/correcao-login-urgente`)** 🩹
    * **Propósito:** Corrigir **bugs críticos identificados em produção** de forma rápida e isolada.
    * **Origem:** Sempre criada a partir da `master`, especificamente da tag da versão que está em produção e precisa da correção.
    * **Destino (Merge):**
        * Após a correção, é mesclada na `master` (e recebe uma nova tag de versão, geralmente incrementando o patch, ex: `v1.0.0` -> `v1.0.1`).
        * Também é mesclada de volta na `develop` (ou na branch `release` ativa, se houver uma) para assegurar que a correção seja incluída nas futuras versões e no desenvolvimento corrente.
    * **Convenção de Nomenclatura:** `hotfix/descricao-breve-do-bug` ou `hotfix/versao-corrigida` (ex: `hotfix/vulnerabilidade-sql-injection`, `hotfix/v1.0.1`).
    * **Ciclo de Vida:** Existe apenas o tempo necessário para aplicar e testar a correção. Após os merges, é excluída.

---

## 🔄 Fluxo de Trabalho na Prática com Exemplos

Vamos visualizar como o Gitflow funciona no dia a dia com alguns cenários comuns.

### Cenário 1: Iniciando o Projeto

1.  **Criação do Repositório e Branch `master`:**
    * Ao criar um novo repositório, a branch `master` (ou `main`) é geralmente a primeira.
    * Um commit inicial pode ser feito (ex: `README.md`, `.gitignore`).

    ```bash
    # No seu diretório de projeto local
    git init
    git add .
    git commit -m "Initial commit: Configuração básica do projeto e README"
    # (Opcional, mas boa prática) Criar uma tag para a versão inicial, mesmo que simples
    git tag -a v0.1.0 -m "Versão inicial do projeto"
    # Enviar para o repositório remoto (ex: GitHub)
    git remote add origin <URL_DO_SEU_REPOSITORIO_REMOTO>
    git push -u origin master --tags
    ```

2.  **Criação da Branch `develop`:**
    * A branch `develop` é criada a partir da `master`.

    ```bash
    git checkout -b develop master
    git push -u origin develop
    ```
    * Neste momento, `master` e `develop` apontam para o mesmo commit.

---

### Cenário 2: Desenvolvendo uma Nova Funcionalidade (`feature`)

* **Contexto:** Precisamos implementar um novo sistema de perfil de usuário.

1.  **Atualizar `develop` e Criar a Branch `feature`:**
    * Antes de iniciar qualquer nova feature, garanta que sua branch `develop` local está sincronizada com a remota.

        ```bash
        git checkout develop
        git pull origin develop
        ```
    * Crie a nova branch `feature` a partir da `develop`:

        ```bash
        # Exemplo de nome: feature/perfil-usuario
        git checkout -b feature/perfil-usuario develop
        ```

2.  **Desenvolver a Funcionalidade:**
    * Realize os commits necessários na sua branch `feature/perfil-usuario`.

        ```bash
        # ... (codifica a funcionalidade) ...
        git add .
        git commit -m "feat: Adiciona estrutura inicial da página de perfil"
        # ... (continua codificando e testando) ...
        git add .
        git commit -m "feat: Implementa edição de dados do perfil do usuário"
        # Envie sua branch feature para o repositório remoto (bom para backup e colaboração/PR)
        git push -u origin feature/perfil-usuario
        ```

3.  **Finalizar a `feature` (Merge para `develop`):**
    * Quando a funcionalidade estiver completa, testada e aprovada (por exemplo, através de um Pull Request):
        1.  **Atualize `develop` novamente** (caso outros colegas tenham integrado novas features):
            ```bash
            git checkout develop
            git pull origin develop
            ```
        2.  **Integre `develop` na sua `feature` (opcional, mas recomendado para resolver conflitos localmente antes):**
            ```bash
            git checkout feature/perfil-usuario
            git pull origin feature/perfil-usuario # Garante que sua branch local está atualizada
            git merge develop
            # (Resolva quaisquer conflitos que surgirem aqui)
            # git push origin feature/perfil-usuario # Atualiza a branch remota com os conflitos resolvidos
            ```
        3.  **Faça o merge da `feature` na `develop`:**
            É **altamente recomendado** usar a flag `--no-ff` (no fast-forward). Isso cria um "merge commit", que agrupa todos os commits da feature e mantém o histórico mais claro, indicando explicitamente que aqueles commits vieram de uma branch específica.

            ```bash
            git checkout develop
            # Se você não fez o passo anterior de integrar develop na feature, faça pull da feature atualizada
            # git pull origin feature/perfil-usuario (se estiver fazendo merge de uma branch remota)
            git merge --no-ff feature/perfil-usuario -m "Merge feature/perfil-usuario: Implementa sistema de perfil de usuário"
            git push origin develop
            ```
        4.  **Excluir a branch `feature`** (para manter o repositório limpo):
            ```bash
            git branch -d feature/perfil-usuario          # Exclui localmente
            git push origin --delete feature/perfil-usuario # Exclui remotamente
            ```

---

### Cenário 3: Preparando um Release (`release`)

* **Contexto:** A branch `develop` acumulou várias funcionalidades (incluindo o perfil de usuário) e está pronta para o lançamento da versão `v1.0.0`.

1.  **Criar a Branch `release` a partir da `develop`:**
    * Garanta que a `develop` local esteja atualizada.

        ```bash
        git checkout develop
        git pull origin develop
        ```
    * Crie a branch `release`:

        ```bash
        # Exemplo de nome: release/v1.0.0
        git checkout -b release/v1.0.0 develop
        git push -u origin release/v1.0.0
        ```
    > **Ponto de Partida:** A partir deste momento, a branch `release/v1.0.0` é "congelada" para novas funcionalidades. Apenas correções de bugs relacionadas a este release e preparativos finais são permitidos.

2.  **Finalizar o Release (Testes Finais, Bug Fixes, Documentação):**
    * Nesta branch, a equipe foca em:
        * Testes exaustivos.
        * Correção de bugs menores descobertos durante os testes.
        * Atualização de documentação (changelog, versão em arquivos de configuração, etc.).

        ```bash
        # Exemplo: Corrigindo um bug visual encontrado nos testes finais
        # ... (faz a correção na branch release/v1.0.0) ...
        git add .
        git commit -m "fix: Corrige alinhamento do rodapé na página de perfil"

        # Exemplo: Atualizar o número da versão em um arquivo de configuração
        # ... (atualiza o arquivo de versão) ...
        git add .
        git commit -m "chore: Atualiza número da versão para 1.0.0 nos arquivos do projeto"
        git push origin release/v1.0.0
        ```

3.  **Merge do Release Finalizado:**
    * Quando a branch `release/v1.0.0` estiver completamente testada, estável e aprovada:
        1.  **Merge na `master`:**
            ```bash
            git checkout master
            git pull origin master
            # Use --no-ff para um histórico claro do merge do release
            git merge --no-ff release/v1.0.0 -m "Merge release/v1.0.0: Lançamento da versão 1.0.0"
            git push origin master
            ```
        2.  **Criar uma Tag de Versão na `master`:**
            É crucial marcar este ponto na `master` com uma tag.

            ```bash
            git tag -a v1.0.0 -m "Release da versão 1.0.0"
            git push origin v1.0.0 # Envia a tag específica
            # ou git push origin --tags # Envia todas as tags locais que não estão no remoto
            ```
        3.  **Merge de Volta na `develop`:**
            Quaisquer correções ou ajustes feitos na branch `release` precisam ser incorporados de volta na `develop` para que não se percam e estejam presentes no próximo ciclo de desenvolvimento.

            ```bash
            git checkout develop
            git pull origin develop
            git merge --no-ff release/v1.0.0 -m "Merge release/v1.0.0: Incorpora ajustes e correções do release na develop"
            git push origin develop
            ```
        4.  **Excluir a branch `release`:**
            ```bash
            git branch -d release/v1.0.0
            git push origin --delete release/v1.0.0
            ```

---

### Cenário 4: Corrigindo um Bug Crítico em Produção (`hotfix`)

* **Contexto:** A versão `v1.0.0` está em produção, mas um bug crítico foi descoberto, impedindo usuários de redefinir suas senhas.

1.  **Criar a Branch `hotfix` a partir da `master` (da tag da versão afetada):**
    * É fundamental criar a branch `hotfix` a partir da tag exata da `master` que está em produção e apresenta o problema.

        ```bash
        git checkout master
        git pull origin master # Garante que a master local está atualizada
        # Cria a hotfix a partir da tag v1.0.0
        git checkout -b hotfix/redefinicao-senha-v1.0.1 v1.0.0
        git push -u origin hotfix/redefinicao-senha-v1.0.1
        ```

2.  **Aplicar a Correção:**
    * Faça a correção do bug e commite na branch `hotfix`.

        ```bash
        # ... (corrige o bug na branch hotfix/redefinicao-senha-v1.0.1) ...
        git add .
        git commit -m "fix: Corrige falha crítica na redefinição de senha"
        # (Opcional, mas comum) Atualizar o número da versão para o patch (ex: v1.0.1) em arquivos de configuração
        # ... (atualiza versão para 1.0.1) ...
        git add .
        git commit -m "chore: Incrementa versão para 1.0.1 devido a hotfix"
        git push origin hotfix/redefinicao-senha-v1.0.1
        ```

3.  **Finalizar o `hotfix`:**
    * Após a correção ser testada e aprovada:
        1.  **Merge na `master`:**
            ```bash
            git checkout master
            git pull origin master
            git merge --no-ff hotfix/redefinicao-senha-v1.0.1 -m "Merge hotfix/redefinicao-senha-v1.0.1: Corrige bug crítico na redefinição de senha"
            git push origin master
            ```
        2.  **Criar uma Nova Tag de Versão na `master`:**
            A correção gerou uma nova versão de patch.

            ```bash
            git tag -a v1.0.1 -m "Versão 1.0.1 - Correção crítica na redefinição de senha"
            git push origin v1.0.1
            ```
        3.  **Merge de Volta na `develop`:**
            Para garantir que a correção também seja incorporada no desenvolvimento contínuo.

            ```bash
            git checkout develop
            git pull origin develop
            git merge --no-ff hotfix/redefinicao-senha-v1.0.1 -m "Merge hotfix/redefinicao-senha-v1.0.1: Incorpora correção da redefinição de senha na develop"
            git push origin develop
            ```
            > **Nota:** Se uma branch `release` estiver ativa no momento do `hotfix`, pode ser necessário fazer o merge do `hotfix` nela também, para que a correção não seja perdida quando o release for finalizado. A decisão depende da urgência e do estado do release.

        4.  **Excluir a branch `hotfix`:**
            ```bash
            git branch -d hotfix/redefinicao-senha-v1.0.1
            git push origin --delete hotfix/redefinicao-senha-v1.0.1
            ```

---

## 🛠️ Comandos Git Essenciais no Gitflow

A tabela abaixo resume os comandos Git mais utilizados em cada etapa do Gitflow:

| Ação                                      | Comando Git                                                                 |
| :---------------------------------------- | :-------------------------------------------------------------------------- |
| Mudar para uma branch existente           | `git checkout <nome-da-branch>`                                             |
| Criar nova branch e mudar para ela        | `git checkout -b <nova-branch> <branch-de-origem>`                          |
| Listar branches locais                    | `git branch`                                                                |
| Listar todas as branches (locais/remotas) | `git branch -a`                                                             |
| Excluir branch local (após merge)         | `git branch -d <nome-da-branch>`                                            |
| Forçar exclusão de branch local           | `git branch -D <nome-da-branch>`                                            |
| Enviar branch local para o remoto         | `git push -u origin <nome-da-branch>` (o `-u` na primeira vez)                |
| Excluir branch remota                     | `git push origin --delete <nome-da-branch>`                                 |
| Atualizar branch local com a remota       | `git pull origin <nome-da-branch>`                                          |
| Mesclar uma branch na branch atual        | `git merge <branch-a-ser-mesclada>`                                         |
| Mesclar com "merge commit" (recomendado)  | `git merge --no-ff <branch-a-ser-mesclada> -m "Mensagem do merge commit"` |
| Criar tag anotada (recomendado)           | `git tag -a <nome-tag> -m "Descrição da tag"`                             |
| Enviar tag específica para o remoto       | `git push origin <nome-tag>`                                                |
| Enviar todas as tags para o remoto        | `git push origin --tags`                                                    |

> **Dica:** Existem ferramentas como `git-flow-avh` (uma extensão do Git) que automatizam muitos desses comandos, simplificando a execução do Gitflow. Podemos explorar seu uso se houver interesse.

---

## ✅ Benefícios Resumidos do Gitflow

* **Clareza e Organização:** Define papéis claros para cada branch, tornando o fluxo de trabalho previsível.
* **Desenvolvimento Paralelo:** Múltiplas features podem ser desenvolvidas em isolamento sem interferências.
* **Estabilidade em Produção:** A branch `master` sempre reflete um estado estável e testado do software.
* **Histórico Significativo:** O uso de merge commits (`--no-ff`) e tags de versão cria um histórico de projeto legível e fácil de auditar.
* **Gerenciamento de Releases Simplificado:** Facilita a preparação, teste e lançamento de novas versões.
* **Manutenção Eficaz:** Permite correções rápidas em produção (`hotfix`) sem interromper o desenvolvimento de novas features na `develop`.

---

## ⚠️ Considerações Importantes

* **Complexidade Inicial:** Para projetos muito pequenos ou equipes de uma única pessoa, o Gitflow pode parecer um pouco burocrático. Nesses cenários, modelos mais simples como o *GitHub Flow* ou *GitLab Flow* podem ser mais adequados.
* **Disciplina da Equipe:** O sucesso do Gitflow depende da compreensão e adesão de todos os membros da equipe ao fluxo estabelecido.
* **Merge Commits:** Embora o `--no-ff` seja crucial para a clareza do histórico, é importante garantir que as mensagens de merge sejam descritivas.

---

## 🤝 Próximos Passos e Dúvidas

Este guia serve como nossa referência principal para o Gitflow. É fundamental que todos se familiarizem com os conceitos e comandos apresentados.

* **Pratiquem:** Tentem simular os cenários descritos em um repositório de teste.
* **Perguntem:** Não hesitem em tirar dúvidas. Uma boa comunicação é chave para o sucesso.
* **Colaborem:** Ajudem uns aos outros a seguir o fluxo corretamente.

Estamos à disposição para discutir qualquer ponto e ajudar na adaptação a este fluxo de trabalho. O objetivo final é termos um processo de desenvolvimento mais eficiente, organizado e colaborativo! 🚀
